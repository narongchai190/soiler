# S.O.I.L.E.R. Secrets Configuration
# ===================================
# Copy this file to secrets.toml and fill in your values:
#   cp secrets.toml.example secrets.toml
#
# IMPORTANT: secrets.toml is gitignored and should NEVER be committed!
#
# NOTE: All secrets are OPTIONAL - the app works without any API keys.
#       It uses OpenStreetMap (free) and deterministic skill-based agents.

# -----------------------------------------------------------------------------
# OPTIONAL: Google Maps API Key
# -----------------------------------------------------------------------------
# Not required - the app uses OpenStreetMap/Folium by default.
# Only needed if you want to switch to Google Maps in the future.
#
# GOOGLE_MAPS_API_KEY = "AIzaSy..."

# -----------------------------------------------------------------------------
# OPTIONAL: OpenAI API Key (for future LLM features)
# -----------------------------------------------------------------------------
# Not required for current functionality.
# The app uses deterministic, rule-based analysis.
#
# OPENAI_API_KEY = "sk-..."

# -----------------------------------------------------------------------------
# OPTIONAL: LINE Messaging API (for future chatbot integration)
# -----------------------------------------------------------------------------
# LINE_CHANNEL_ACCESS_TOKEN = "..."
# LINE_CHANNEL_SECRET = "..."
